#### Docker and compose.yml

* Maybe we can improve it and add some functionality. Right now the one from practise sessions is used
* `docker compose -f project_4/compose.yml up -d`
* when in project_4 directory then `docker compose -f compose.yml up -d`


* Review Jupyter UI (localhost:8888), run the example notebook
* Review Spark UI (localhost:4040)